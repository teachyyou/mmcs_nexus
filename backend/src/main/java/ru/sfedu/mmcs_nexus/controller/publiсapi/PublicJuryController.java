package ru.sfedu.mmcs_nexus.controller.publiсapi;

import org.springframework.web.bind.annotation.RestController;

@RestController
public class PublicJuryController {

//    @GetMapping(value = "/api/v1/admin/events/{id}/projects", produces = "application/json")
//    public ResponseEntity<Map<String, Object>> getJuriesByEvent(@PathVariable("id") UUID id, Authentication authentication) {
//
//        List<Project> projects = projectEventService.findByEventId(id);
//
//        Map<String, Object> response = new HashMap<>();
//        response.put("content", projects);
//        response.put("totalElements", projects.size());
//
//        return ResponseEntity.ok().body(response);
//    }
}
